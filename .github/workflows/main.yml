name: Deploy App

on:
  push:
    branches:
      - main

jobs:
  call-deploy:
    uses: leoserverteam/DevOpsTemplates/.github/workflows/onlycompose.yml@main
    with:
      project_name: "Logs"
      extra-files: "promtail-config.yaml"

  call-nginx:
    uses: leoserverteam/DevOpsTemplates/.github/workflows/deploynginx.yml@main
